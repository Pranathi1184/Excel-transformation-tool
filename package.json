{
  "name": "excel-transformation-tool",
  "private": true,
  "version": "1.0.0",
  "description": "Excel Data Transformation Tool - full stack",
  "scripts": {
    "start": "concurrently -n backend,frontend -c blue,green \"node scripts/start-backend.js\" \"npm run dev --prefix frontend\"",
    "start:backend": "node scripts/start-backend.js",
    "start:frontend": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
